#include <stdio.h>
#include <stdlib.h>

/**
 * main - main function
 *
 * Return: void
 */
int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i += 1;
	}
	printf("\n");
	return (0);
}
