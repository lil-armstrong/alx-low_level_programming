#include "main.h"

/**
 *_puts - prints a string, followed by a new line, to stdout.
 * @str: array pointer of type char
 *
 * Return: int
 */

void _puts(char *str)
{
	int n = 0;

	while (*(s + n))
	{
		_putchar(*(s + n));
		n++;
	}
	_putchar(10);
	return (n);
}

